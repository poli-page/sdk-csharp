# Normalize all text files to LF in the index, and check them out as LF on every
# platform. Without this the Windows CI runner (core.autocrlf=true by default)
# converts every .cs file to CRLF on checkout, tripping dotnet format's
# ENDOFLINE check.
* text=auto eol=lf
